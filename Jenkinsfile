pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo "hello"
         //       echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
            }
        }
    }
}
